import 'package:flutter/material.dart';
/*
// Rosé Pine Dark Theme Colors
class RosePineColors {
  static const base = Color(0xFF191724);
  static const surface = Color(0xFF1f1d2e);
  static const overlay = Color(0xFF26233a);
  static const muted = Color(0xFF6e6a86);
  static const subtle = Color(0xFF908caa);
  static const text = Color(0xFFe0def4);
  static const love = Color(0xFFeb6f92);
  static const gold = Color(0xFFf6c177);
  static const rose = Color(0xFFebbcba);
  static const pine = Color(0xFF31748f);
  static const foam = Color(0xFF9ccfd8);
  static const iris = Color(0xFFc4a7e7);
}
*/

// Google Dark Theme Colors with Subtle Tint
class RosePineColors {
  // Dark theme background colors with subtle blue-purple tint
  static const base =
      Color(0xFF1E2029); // Slightly blue-purple tinted dark background
  static const surface = Color(0xFF262733); // Slightly lifted surface with tint
  static const overlay = Color(0xFF373945); // Card/overlay with matching tint

  // Text and muted colors
  static const muted = Color(0xFF9AA0A6); // Muted text
  static const subtle = Color(0xFFBDC1C6); // Secondary text
  static const text = Color(0xFFE8EAED); // Primary text

  // Google's primary colors adapted for dark theme
  static const love = Color(0xFFEA4335); // Google Red
  static const gold = Color(0xFFFBBC04); // Google Yellow
  static const rose = Color(0xFFDB4437); // Google Red (alternative)
  static const pine = Color(0xFF4285F4); // Google Blue
  static const foam = Color(0xFF34A853); // Google Green
  static const iris = Color(0xFF8AB4F8); // Light Blue (accent)
}
